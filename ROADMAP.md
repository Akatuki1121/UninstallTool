# UninstallTool 開発ロードマップ

## ゴール
Geek Uninstallerを超える、残存ファイル検出精度の高いWindowsアンインストーラーを作る。
常駐なし、外部ツール依存なし(単一exe)、無料版/有料版で機能分割。

## 実装順序

### Phase 0: 基盤 [完了]
- [x] OperationLog: 操作ログ/状態トラッカー、エラー時に文脈付きレポートを自動生成
- 動作確認済み(2026-08-16)

### Phase 1: インストール済みアプリ一覧取得 [完了]
- [x] レジストリ `HKLM\Software\Microsoft\Windows\CurrentVersion\Uninstall`
- [x] `HKLM\Software\WOW6432Node\...\Uninstall` (32bitアプリ)
- [x] `HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall` (ユーザー単位インストール)
- [x] 各アプリの DisplayName, DisplayVersion, Publisher, InstallLocation, UninstallString を取得
- [x] OperationLogに各ステップを記録
- 動作確認済み(2026-08-16): 実機で112件のアプリを正しく検出

### Phase 2: 通常アンインストール実行 [完了]
- [x] 選択したアプリの UninstallString を実行(既存の公式アンインストーラーを呼ぶ)
- [x] 完了待機、終了コード確認
- [x] OperationLogに記録
- [x] 安全のためdryRunフラグを実装(既定true)。テスト用使い捨てアプリができるまで実行はしない
- 動作確認済み(2026-08-16): 実機の112アプリ中10件でUninstallStringパースをドライラン確認、複雑なパス(空白入り・引数付き)も正しく分解できることを確認

### Phase 3: 簡易UI (WPF) [完了]
- [x] プロジェクトを UninstallTool.Core(ロジック) / UninstallTool.UI(WPF) / UninstallTool(コンソール、テストランナー) の3つに分離
- [x] アプリ一覧表示(Phase1のAppInventoryをそのまま利用)
- [x] 一覧から選択 → アンインストールボタン(Phase2のAppUninstallerをそのまま利用、UIのチェックボックスでdryRun切替可能)
- [x] 操作ログをウィンドウ内に表示
- 動作確認済み(2026-08-16): 実機でウィンドウ起動・アプリ選択・ドライラン実行(結果ダイアログ「DryRun」表示)を確認
- Geek Uninstaller相当のシンプルな操作感を最低限で実現
- この時点ではまだ残存物検出機能はなし(見た目だけ先行)

### Phase 4: MFT高速検索エンジン [完了]
- [x] USN Journal (`FSCTL_ENUM_USN_DATA`) を使った自前実装(P/Invoke、外部依存ゼロ)
- [x] 管理者権限チェック、非対応ボリュームのハンドリング(警告ログを出して安全にスキップ)
- [x] アプリ名/パブリッシャー名でファイルパスを高速検索(部分一致、大文字小文字無視)
- [x] ParentFileReferenceNumberを辿ったフルパス復元、循環参照の安全な検知
- 動作確認済み(2026-08-16、管理者権限で実機実行): C:ドライブ全体(273万9929件)を28秒でスキャン、検索語「Obsidian」で94件を正しいフルパスで検出。深い階層・空白入りパスも正確に復元できることを確認

### Phase 5: 残存物スキャン(横断)+ 選択削除 [実装完了、実機確認待ち]
- [x] AppData / LocalAppData / ProgramData 配下の関連フォルダ(MFT検索経由、オプトイン)
- [x] レジストリ全キー横断検索(Uninstallキー以外も、Software配下)
- [x] サービス登録の確認
- [x] タスクスケジューラのエントリ確認(レジストリTaskCache経由)
- [x] 環境変数PATHの該当パス確認
- [x] スタートアップ項目(レジストリRun + スタートアップフォルダ)確認
- [x] ResidueRemover: カテゴリごとの削除実装(レジストリ削除/sc.exe delete/schtasks.exe delete/PATH編集/ファイル削除)、既定dryRun
- [x] UI: 「残存物をスキャン」ボタン→チェック付き一覧ウィンドウ(全選択/全解除/選択削除)、MFT検索は重いためオプトインチェックボックス+非同期実行
- ビルド成功(2026-08-16) — 実機での動作確認待ち(コンソール版でのScanAll確認、UI版でのスキャン→選択削除の一連の流れ確認)
- 動作確認済み(2026-08-16、管理者権限で実機実行): 架空アプリ名で全カテゴリ横断スキャンを実行し0件(誤検出なし)を確認

### Phase 6: 孤児候補の相関ロジック [実機確認済み、実用レベルまで精度改善]
- [x] MftSearchEngineに ListSubdirectories を追加(指定フォルダ直下の子フォルダを高速列挙)
- [x] OrphanDetector: Program Files / Program Files (x86) / AppData\Roaming / AppData\Local 直下を走査
- [x] インストール済みアプリの InstallLocation と名前ベースで突き合わせ、一致しないものを孤児候補として抽出
- [x] Windows自体の既知フォルダ(Microsoft, Packages, WindowsApps等)は除外リストで除外
- [x] 最終アクセス日時のような曖昧な指標は不採用(誤検出が多いとの指摘を踏まえた設計判断)
- [x] 削除は行わず候補提示のみ(誤検出リスクの高いカテゴリのため)
- 動作確認済み(2026-08-16、管理者権限で実機実行): C:ドライブ全体を走査、初回48件検出
- 誤検出の原因調査と修正(2026-08-16):
  - [x] 短い名前(例: "Git")が部分一致の最小文字数制限で無条件除外される不具合を修正(短い名前同士は完全一致のみ許可する救済ロジックに変更)
  - [x] トークン単位の部分一致を追加(例: "Arduino15" と "Arduino IDE 2.3.10" のように文字列として互いを含まないが単語を共有するケースを救済。キャメルケース・数字境界で分割、4文字未満/数字のみのトークンは除外)
  - [x] OneDriveをKnownNonAppFolderNamesに追加(per-user配布のためUninstallレジストリキーに登録されず、名前相関では原理的に検出不可能と判明)
  - [x] DisplayNameとフォルダ名が単語レベルで一切重ならないケース(例: "Epic Games Launcher" と "UnrealEngine")向けに、キュレート済みエイリアス表(KnownAppFolderAliases)を追加
  - [x] さらに拾いきれないケース向けに、フォルダ内exe/dllのFileVersionInfo(CompanyName/ProductName)を既知アプリのDisplayName/Publisherとトークン一致させる2段目チェックを実装(includeExecutableMetadataCheck、オプトイン)。名前相関で除外しきれなかった候補のみに適用することで速度への影響を抑制
- 動作確認済み(2026-08-16、管理者権限で実機実行): 修正後は48件→25件まで削減。Git/OneDrive/Fusion360/Arduino15/UnrealEngine系/Android等、現役インストール済みアプリの誤検出をすべて解消したことを確認
- 既知の残課題: レジストリに一切登録されない配布形態(OneDriveのようなケース)は個別に除外リストで対応するしかなく、汎用的な解決策はない(業界の他ツールでも確認できなかった限界と一致)

### Phase 7: UI拡張 + 自己アンインストール機能 [完了]
- [x] MainWindowに「孤児候補をスキャン(実験的)」ボタンを追加
- [x] OrphanWindow: 検出結果をチェック付き一覧表示、削除は既存のResidueRemover(MftFileカテゴリ)に委譲して一本化
- [x] 「エクスプローラーで開く」ボタンで削除前に中身を目視確認できる導線を追加(誤検出リスクへの安全策)
- [x] 管理者権限チェック(ElevationChecker)+ 未昇格時の警告バナー(サイレント0件事故の再発防止)
- [x] 操作ログをTextBox化してコピー可能に
- [x] SelfUninstaller: batファイル経由の自己削除実装(2段階確認ダイアログ付き)。BAT生成内容の自動テスト済み。実機削除は配布用コピーでリリース前に確認する
- ビルド成功(2026-08-16、Core/UI両方)

### Phase 8: 無料版/有料版の分岐実装 [実装完了]
- [x] LicenseState: Pro機能ゲートの窓口を一本化(現状は常にfalseを返すスタブ、将来ライセンスキー検証に差し替え可能な設計)
- [x] AppListViewを複数選択可能に変更(SelectionMode=Extended)
- [x] 「複数アプリを一括アンインストール(Pro)」ボタン: LicenseState.IsProUnlockedがfalseの間は案内のみ表示、実行しない
- [x] 無料版: 1アプリずつのフル機能(Phase1〜7全部込み)は従来通りすべて利用可能
- ビルド成功(2026-08-16、Core/UI両方)
- [x] スキャン結果エクスポート(将来のPro機能候補、残存物/孤児候補のCSV出力を実装済み)
- 未着手: 除外リスト保存(将来のPro機能候補として保留)
- 未着手: 実際のライセンスキー発行・検証の仕組み(決済含め、配布戦略が固まってから着手)

## 技術メモ
- .NET 8 / C# / WPF
- 単一exe化(自己完結デプロイ)を最終的に検討
- 管理者権限が必要な処理(USN Journal等)はマニフェストで要求するか、実行時にUAC昇格を促す設計にする

## 進捗ログ
- 2026-08-16: Phase 0完了。OperationLogの動作確認済み(意図通りエラー文脈を生成できることを確認)
- 2026-08-16: Phase 5/6を実機で初確認(管理者権限必須と判明)。Phase6で孤児候補48件検出、うちGit/OneDrive/Fusion360/Arduino15/UnrealEngine系等の現役アプリ誤検出を特定
- 2026-08-16: 誤検出修正(短い名前の完全一致救済、トークン一致、既知除外リスト拡充、エイリアス表、exe/dllメタデータによる2段目チェック)を実装・実機確認。48件→25件に削減、既知の誤検出はすべて解消
- 2026-08-16: Phase 7(孤児候補UI統合、管理者権限チェック+警告バナー、ログのコピー対応、自己アンインストール)完了
- 2026-08-16: Phase 8(複数選択+一括アンインストールのPro機能ゲート)完了。全Phase(0〜8)の実装が完了
- 2026-08-16: アプリ一覧にアイコン表示追加(AppIconResolver: DisplayIcon→UninstallStringのexe→InstallLocation内のexeの順に探索)
- 2026-08-16: 開発者向けProアンロック追加(LicenseState: exeフォルダのdev_unlock.flagで自分だけPro機能を使える。配布物には含めない)
- 2026-08-16: 孤児候補にサイズ・ファイル数・最終更新日時を追加(OrphanCandidate.FileCount/TotalSizeBytes/LastModified)。判断材料として提示するのみで自動判定には使わない
- 2026-08-16: UIデザインを試行錯誤(ダークテーマ+カード形式→見やすさ優先で明るい標準テーブル形式に最終決定。Geek Uninstaller等の見慣れたレイアウトに寄せた)
- 2026-08-17: WPF-UI(lepoco/wpfui、MITライセンス)導入、Fluent Design化。メニューバー(ファイル/表示/ツール/孤児候補スキャンへの導線含む)追加。THIRD_PARTY_LICENSES.md作成(未同梱)
- 2026-08-17: 画面遷移をユーザー目線で見直し。孤児候補スキャンボタンをメイン画面から撤去しメニュー「ツール」に一本化、単体アンインストール後に残存物スキャンを自動提案する流れに変更、一括アンインストールボタンは複数選択時のみ表示に変更
- 2026-08-17: 操作ログにコピーボタン追加(範囲選択に頼らない)
- 2026-08-18: 起動パフォーマンス改善(アイコン抽出・レジストリ読み取りの非同期化、Mica背景削除)
- 2026-08-18: 起動時クラッシュの原因判明・修正(ToggleSwitchのIsChecked初期値によりInitializeComponent実行中にChecked/Uncheckedハンドラが発火し、未初期化フィールド参照でNullReferenceException。nullガードで解消)。イベントログ調査で特定
- 2026-08-18: アプリアイコンにフォールバック追加(見つからない場合はshell32.dllの汎用アイコン)、開発者向けProアンロック(dev_unlock.flag)追加
- 2026-08-18: ResidueWindow/OrphanWindowもWPF-UI Fluent化、3画面の見た目を統一
- 2026-09-04: UI改善および安全性の強化
  - 残存物/孤児候補画面のチェックボックス依存を廃止し、ListView標準の行選択(Ctrl/Shift複数選択)へ移行
  - 単体アンインストール完了後、ダイアログを挟まず自動で残存物スキャン画面へスムーズに移行するよう変更
  - レジストリ/exe等の会社名(CompanyName)からのPublisher補完ロジック(AppPublisherResolver)を追加
  - NameMatcherをResidueScanner全体へ適用し、短いアプリ名での誤検出リスクを防止
  - THIRD_PARTY_LICENSES.mdのビルド時自動コピー設定を追加
  - バージョン情報ダイアログの文言整理
- 2026-09-04: GitHub Issueによる不具合報告・機能要望の送信画面を追加。不具合/要望の種別、概要、再現手順または利用場面、期待する動作を入力し、環境情報と任意の操作ログを添付できるようにした
- 2026-09-04: メイン画面の視覚階層を調整。アプリ一覧の通常文字色・選択/ホバー状態を整理し、列ヘッダーを統一、操作ログを初期状態で折りたたむようにした。UIビルド確認済み
- 2026-09-04: メイン画面を一覧中心に再構成。アプリ名・バージョン・発行元・インストール場所を横断検索できる検索欄と表示件数を追加し、孤児スキャン/自己アンインストールをメニューへ集約。UIビルド確認済み
- 2026-09-04: 孤児候補の除外リスト保存を追加。ユーザー単位のLocalAppData配下へJSON保存し、検出画面の「今後表示しない」から再スキャン以降の候補を除外できるようにした。自動テスト・UIビルド確認済み

## 未実装・要対応(会話で挙がったが手つかずのもの)
- [x] 単体アンインストール後に残存物スキャンを自動提案 ではなく、提案なしで移行へ。 (完了)
- [x] アプリ等を消す際、チェックボックスを使わないようにする (完了: ListView行選択へ移行)
- [x] 単体アンインストール後の残存物スキャンで、選んだアプリと無関係な結果が混ざる懸念 (完了: NameMatcherをResidueScanner全体へ適用)
- [x] THIRD_PARTY_LICENSES.mdを作成したが、配布物(exeと同じフォルダ)に同梱する仕組みが未実装 (完了: csprojにCopyToOutputDirectoryを設定)
- [x] バージョン情報から、「Geek Uninstallerを超えることを目指した」を削除する (完了)
- [x] 複数(一括)アンインストール後の残存物スキャン自動提案(成功した各アプリを順次スキャンし、アプリ名付きで結果を表示)
- [x] SelfUninstallerの自動テスト(BAT生成内容を検証)。実機削除テストは配布用コピーでのみ実施するため、リリース前確認として継続
- [x] ビルド時のNU1510警告確認(2026-09-04のDebugビルドで警告なし。System.Drawing.Commonの直接参照もなし)
- [x] GitHub Issueによる不具合報告・機能要望の送信(入力フォーム、環境情報、任意の操作ログ、種別ラベル付きのIssue下書き)
- [x] 孤児候補の除外リスト保存(LocalAppData配下へのJSON保存と検出画面からの登録)

## 次にやるとよさそうなこと(未着手)
- [x] スキャン結果エクスポート(Pro機能候補、残存物/孤児候補のCSV出力を実装済み)
- [x] 除外リスト保存(Pro機能候補、孤児候補向けを実装済み)
- 実際のライセンスキー発行・検証の仕組み、決済導線
- 単一exe化(Native AOT等での軽量化、配布のしやすさ向上)
- メイン画面のデザイン刷新の残作業(メニューのツールバー化、ログの詳細パネル化、空状態/読み込み状態の専用表示)
- Windows以外の環境での動作確認は対象外(このツールはWindows専用)


